const initialState = {
  errorDisplay: false,
  loadingDisplay: true,
};

const Ui = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Ui;
